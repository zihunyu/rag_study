# RAG 知识库项目需求与架构总结

结合你上传的《企业级 RAG 知识库架构设计说明书》和这次补充的 18 项要求，你的项目可以概括为：

> **使用 Python 3.12 开发后端、Vue 3 开发前端，以 MySQL 管理知识和业务数据，以 Milvus 管理向量索引，通过统一的 AI 接入层调用解析、OCR、语音转写、Embedding、重排序和大语言模型，构建一个支持多格式文档、混合检索、证据引用、版本管理、权限控制和质量评测的知识库系统。**

项目不只是“上传文件后聊天”，而是包含**知识治理、知识加工、知识检索、可信生成、持续评测**五类能力。这也是附件确定的整体范围。

下面将内容整理为：**确定的技术选型、目录结构、模块职责、数据处理流程、关键规则，以及尚未确定的事项。**

---

## 一、已经明确的技术选型与项目范围

### 1. 技术选型

| 项目     | 本项目采用的要求            | 需要注意的边界                                           |
| ------ | ------------------- | ------------------------------------------------- |
| 后端开发语言 | **Python 3.12**     | 后端框架尚未明确，不默认替你选定                                  |
| 前端框架   | **Vue 3**           | 负责知识管理、任务状态、搜索、问答和来源展示                            |
| 关系数据库  | **MySQL**           | 保存文档目录、版本、权限、任务、配置记录、评测等管理数据                      |
| 向量数据库  | **Milvus**          | 保存向量及检索所需的关联字段，承担向量检索                             |
| 文档解析接入 | **MinerU**          | 你指定其用于 Word、PPT、PDF 等解析；具体版本、实际格式覆盖及是否需要转换，材料尚未给出 |
| 模型接入方式 | 统一放入 **`ai` 模块**    | 包括大语言模型、OCR、Embedding、Reranker、语音转写等能力            |
| 检索方式   | **关键词/BM25 + 向量检索** | 使用 RRF 融合，再进行重排序；BM25 的具体实现组件尚未确定                 |
| 原始文件保存 | 保留原文件、规范化结果和相关媒体    | 具体采用本地文件存储还是对象存储，尚未确定                             |

附件里的 PostgreSQL 等架构属于通用选型示例；这次总结将你明确提出的 **MySQL + Milvus** 作为项目约束，不继续沿用之前的 PostgreSQL、pgvector 或 Elasticsearch 组合。

### 2. 功能范围

系统需要覆盖以下完整过程：

**文档接入 → 解析与清洗 → 知识组织 → Chunk 切分 → 向量化与索引 → 混合检索 → 证据组织 → 生成与引用验证 → 更新、删除、权限与评测。**

支持范围包括你提出的 Markdown、TXT、Word、PPT、PDF、图片、HTML、Excel、CSV 和语音内容。

附件另外提出了邮件、工单、代码和数据库视图等知识源。这些可以保留为扩展范围，但不应在总结中当成已经确定了接入方式的功能。

---

## 二、十个核心文件夹：职责应该怎样划分？

你的十个目录方向可以保留。最需要处理的是：**不同目录之间不能重复承担同一项工作。**

下面的英文目录名是对你中文模块的工程化命名建议。

| 编号 | 目录                           | 核心职责                                             | 职责边界                              |
| -- | ---------------------------- | ------------------------------------------------ | --------------------------------- |
| 1  | **`ai/`**                    | 接入 LLM、OCR、MinerU、Embedding、Reranker、语音转写等能力     | 负责“怎样调用模型或智能解析服务”，不负责整条入库流程       |
| 2  | **`document_processing/`**   | 文件识别、解析路由、结构提取、OCR/语音处理调度、清洗、规范化、Chunk 切分、解析质量检查 | 负责“怎样把文件变成可靠知识片段”，不直接承担索引发布       |
| 3  | **`knowledge_modeling/`**    | 知识分类、来源、文档、版本、章节、元数据、适用范围、权限规则、有效期和生命周期          | 负责“知识是什么、从哪里来、什么时候有效、谁可以使用”       |
| 4  | **`information_retrieval/`** | 关键词检索、分词、BM25、向量检索调用、Top-K、单路候选结果和检索分数           | 提供基础检索能力，不负责完整问答流程                |
| 5  | **`vector_indexing/`**       | 向量化任务、输入规范、维度检查、距离函数、Milvus 索引管理、HNSW 配置、索引版本和重建 | 调用 `ai/embedding` 获取向量，不再重复实现模型接入 |
| 6  | **`rag_orchestration/`**     | 问题处理、检索条件、双路召回、RRF、去重、重排序、父子片段扩展、上下文预算           | 负责“按什么顺序组合检索能力”                   |
| 7  | **`generation_control/`**    | 提示词、证据约束、结构化答案、引用验证、拒答、澄清和冲突披露                   | 调用 `ai/llm`，不把权限判断交给模型            |
| 8  | **`engineering_security/`**  | 后台任务、幂等、发布协调、鉴权、缓存、日志、监控、限流、备份及基础设施接入            | 提供横向工程能力，避免变成堆放所有业务代码的目录          |
| 9  | **`evaluation/`**            | 数据集、检索评测、答案评测、引用评测、安全测试、时延与费用分析、实验对比             | 负责证明系统是否正确、改动是否有效                 |
| 10 | **`configs/`**               | 应用、模型、解析、切分、检索、索引、安全和评测配置                        | 保存配置定义和非敏感参数，不直接保存真实密钥            |

这种模块拆分可以先在同一个后端项目中实现，不意味着一开始就拆成十个微服务。附件也明确提出，先划分服务契约，早期可以同进程实现，规模增大后再独立部署。

### 最容易重复的三个边界

**模型接入与向量索引分开。**

`ai/embedding` 负责把文本转换成向量；`vector_indexing` 负责检查向量、写入 Milvus、建立索引和管理索引版本。你列出的“Embedding 模型”和“向量模型”，在这套目录中按文本向量化能力统一归入 `ai/embedding`。

**解析能力接入与文档处理分开。**

`ai/mineru` 负责请求解析能力并接收结果；`document_processing` 决定什么文件走什么策略，并把结果转换成系统统一的数据结构。这里将 `ai` 作为“模型及智能解析能力适配层”，并不表示所有接入对象都是同一种模型。

**基础检索与 RAG 编排分开。**

基础检索返回候选片段；RAG 编排决定如何合并候选、调用重排序、补充父片段以及组装证据。

---

## 三、建议的工程目录

下面是上述职责的一种落地结构。除你的十个模块外，补充了 API、公共数据契约、迁移、测试、前端和部署目录；这些辅助职责在附件的工程目录中也有对应安排。

```text
rag-knowledge-base/
├── backend/
│   ├── src/
│   │   ├── ai/                         # 1. 模型与智能解析能力接入
│   │   │   ├── contracts/              # 统一输入输出接口
│   │   │   ├── llm/
│   │   │   ├── ocr/
│   │   │   ├── mineru/
│   │   │   ├── embedding/
│   │   │   ├── reranker/
│   │   │   └── speech/                 # 语音转写能力接入
│   │   │
│   │   ├── document_processing/        # 2. 文档处理
│   │   │   ├── detection/
│   │   │   ├── parsers/
│   │   │   ├── normalization/
│   │   │   ├── chunking/
│   │   │   └── quality/
│   │   │
│   │   ├── knowledge_modeling/         # 3. 知识组织与建模
│   │   ├── information_retrieval/      # 4. 基础检索
│   │   ├── vector_indexing/            # 5. 向量化与 Milvus 索引
│   │   ├── rag_orchestration/          # 6. RAG 检索编排
│   │   ├── generation_control/         # 7. 生成与引用控制
│   │   ├── engineering_security/       # 8. 工程化与安全
│   │   ├── evaluation/                 # 9. 评测与优化
│   │   │
│   │   ├── api/                        # HTTP 接口与请求处理
│   │   └── schemas/                    # 文档、片段、查询、答案契约
│   │
│   ├── configs/                        # 10. 配置文件
│   ├── migrations/                     # MySQL 数据库迁移
│   ├── tests/
│   └── pyproject.toml
│
├── frontend/                           # Vue 3
│   └── src/
│       ├── views/
│       ├── components/
│       ├── api/
│       └── router/
│
├── deploy/                             # 部署与环境配置
└── docs/                               # 架构、接口、运维说明
```

这里的目录只是代码组织，**不等于业务知识分类**。代码目录按职责划分，知识库里的文档则需要按产品、主题、组织、地区等维度分类。

---

## 四、知识组织与数据存储

### 1. 知识层级

附件给出的知识组织结构可以继续使用：

```text
Tenant：租户 / 组织
└── KnowledgeSpace：知识空间
    └── Corpus：语料集合
        └── Source：知识来源
            └── Document：文档的稳定身份
                └── DocumentVersion：文档版本
                    └── Section：章节或结构节点
                        └── Chunk：检索片段
```

分类、标签、术语、同义词和权限策略关联在这套结构上，而不是只靠一个文件夹路径表达所有分类关系。

需要明确三个不同对象：

**Document** 表示“是哪份文档”；**DocumentVersion** 表示“这份文档的哪一版”；**Chunk** 表示“这一版本中用于检索的哪一段”。

### 2. MySQL、Milvus 和文件存储的分工

| 存储位置        | 保存什么                                  | 定位             |
| ----------- | ------------------------------------- | -------------- |
| **文件存储**    | 原文件、网页快照、音频、图片、规范化文档、解析产物             | 保存可以回查、重新加工的资料 |
| **MySQL**   | 知识目录、来源、文档版本、章节、权限、生命周期、任务、处理血缘、评测与反馈 | 管理知识状态与业务关系    |
| **Milvus**  | 向量、Chunk 关联标识，以及检索所需的过滤字段             | 可重建的检索索引       |
| **缓存，组件待选** | 临时查询结果、向量化结果或其他可复用结果                  | 性能辅助，不是唯一事实来源  |

这延续了附件的原则：**原始资料和规范化资料必须保留，向量库不能成为知识的唯一副本。**

### 3. 需要保存的统一数据

你的输入和附件共同要求，至少保留下面几组信息：

| 类别       | 需要保留的内容                                          |
| -------- | ------------------------------------------------ |
| **身份**   | 租户、知识空间、来源、文档 ID、版本 ID、章节 ID、Chunk ID、父 Chunk ID |
| **内容**   | 内容块类型、原始文本或原文引用、展示文本、检索文本、章节路径                   |
| **定位**   | 来源地址、页码、章节、字符或坐标范围；语音内容对应起止时间                    |
| **表格结构** | 表名、表头、列名、单位、行列关系、必要脚注                            |
| **治理**   | 分类、权威等级、适用条件、权限、密级、生效时间、失效时间、生命周期状态              |
| **处理血缘** | 文件哈希、解析器版本、解析配置版本、分块版本、模型版本、索引版本                 |
| **质量**   | 解析异常、乱码或空内容标记、OCR 质量信息及人工复核状态                    |

附件将这些信息归纳为身份、溯源、权限与时态、处理血缘四组不可省略的元数据。

---

## 五、文档处理策略总结

**总原则：统一输出，不强求所有格式都走同一个解析过程。**

| 文档类型               | 本项目的处理要求                          | 重点保留与检查                       |
| ------------------ | --------------------------------- | ----------------------------- |
| **Markdown / TXT** | 按标题、段落、列表和代码块解析                   | 标题层级、代码完整性、编码                 |
| **Word**           | 提取段落、标题、列表和表格；按你的要求纳入 MinerU 接入目标 | 样式不规范、表格与正文关系、来源定位            |
| **PPT**            | 纳入你指定的 MinerU 接入目标，按页组织内容         | 幻灯片页序、标题、文本、表格、图片关联；具体输出结构需确认 |
| **有文本层的 PDF**      | 优先提取文本和版面结构                       | 双栏顺序、页眉页脚、断行、页码               |
| **扫描 PDF / 图片**    | 对缺乏可靠文字的页面或区域执行 OCR               | 数字、单位、专有名词、坐标和识别质量            |
| **HTML**           | 提取正文、标题、表格和来源，清理导航噪声              | 原始 URL、抓取时间、正文快照、不可信脚本和内容     |
| **Excel / CSV**    | 保留结构化字段和行列关系                      | 表头、单位、数据口径；不能仅靠文本片段完成精确统计     |
| **语音**             | 转写为文本并进行分段，保留原音频                  | 起止时间、转写质量、文本与原音频的对应关系         |

附件特别要求：保留原文件和规范化结果、网页快照、音视频时间戳，以及可回到原始位置的证据。

**MinerU 的接入目标已经明确，但材料没有提供实际格式支持矩阵。** 因此，Word/PPT/PDF 是否直接处理、是否需要中间转换、转换后如何保留定位，都应作为待验证项，而不是写成已经确认的能力。

---

## 六、完整处理流程

### 1. 入库流程

```text
上传文件 / 接入知识源
        ↓
身份与上传权限检查
        ↓
文件类型识别、原件保存、哈希与重复检查
        ↓
创建文档及不可变版本
        ↓
按类型调用解析、OCR 或语音转写能力
        ↓
生成统一结构化文档
        ↓
清洗、章节识别、表格与代码处理
        ↓
Chunk 切分、元数据与权限绑定
        ↓
质量检查
        ↓
Embedding、关键词索引与 Milvus 索引
        ↓
索引验证与抽样检索
        ↓
发布为可用版本
```

这里要保留**质量检查和隔离处理**：解析失败、缺少权限、内容为空、定位无效等资料，不能直接进入生产检索。附件也要求每一步幂等，失败可见、可重试，超过阈值后进入明确的异常处理流程。 

### 2. 查询与问答流程

```text
用户问题
    ↓
认证并计算允许访问的知识范围
    ↓
保留原问题，处理术语、产品、版本和时间条件
    ↓
关键词/BM25 召回 ──┐
                  ├── 使用同一套权限、版本和有效期过滤
Milvus 向量召回 ──┘
    ↓
核验候选的当前可用状态
    ↓
RRF 融合、去重
    ↓
Reranker 重排序
    ↓
按需补充同权限、同适用版本的父片段
    ↓
组织证据与控制上下文预算
    ↓
根据证据生成答案
    ↓
引用、权限、有效性和输出检查
    ↓
返回答案、来源、状态与 Trace ID
```

需要区分：

**检索阶段的权限过滤是必需措施；后续再次核验是补充防线，不能替代检索前或索引内过滤。** 附件明确要求两路召回使用同一授权条件，并禁止把“先召回越权资料再删除”作为权限方案。 

---

## 七、Chunk、Embedding 与检索参数

### 1. Chunk 切分规则

你和附件的方向一致：

**默认采用结构切分，固定长度作为基线或兜底，语义切分用于实验，父子切分用于补充上下文。**

需要保留的具体规则是：

| 内容    | 切分要求                          |
| ----- | ----------------------------- |
| 制度与规则 | 条件、主体规则、限制和例外尽量一起保留           |
| 表格    | 拆分后仍保留表头、单位、表名和必要注释           |
| 代码与接口 | 尽量保留函数、参数说明、示例之间的关系           |
| FAQ   | 问题和完整答案作为一个语义单元               |
| 父子片段  | 子片段用于定位，父片段用于补充；不能跨权限、跨适用版本扩展 |
| 超长内容  | 继续按结构、段落或句子处理，不能静默丢弃尾部内容      |

附件同样强调先按语义结构切分，结构节点过大时再按句子或 Token 切分。

### 2. 两份材料中的参数存在差异

这些差异需要保留，不能悄悄合并成一组“已经确定的最优参数”。

| 参数                | 你本次输入                                 | 上传附件                        |
| ----------------- | ------------------------------------- | --------------------------- |
| 普通正文 / 子 Chunk 长度 | 约 **400—800 Token**                   | 子 Chunk 约 **300—600 Token** |
| 重叠长度              | 连续正文约 **50—100 Token**；完整短段落、FAQ 可不重叠 | 相邻重叠约 **10%—15%**           |
| 父 Chunk 长度        | 未给定具体值                                | 约 **800—1,500 Token**       |
| 每路召回量             | 关键词、向量各 **Top 30**                    | 每路 **Top 40—80**            |
| 进入重排序的候选          | 约 **30—40 条**                         | 约 **20—50 条**               |
| 最终证据              | 约 **4—8 个片段**                         | 约 **5—12 个片段**              |

附件明确把这些数字定义为起始参数，而不是行业标准。建议在配置中保留不同实验方案，使用同一评测集比较后再确定项目默认值。 

### 3. Embedding 与索引必须一起版本化

根据你的要求，至少记录：

```text
embedding_model_id
model_revision
embedding_dimension
输入预处理配置
查询指令或前缀配置
是否归一化
距离函数
chunking_version
index_generation
```

模型接入、输入处理、分块策略、索引版本要能够对应起来。

**不能只替换查询端模型，却继续使用另一套模型生成的旧向量。** 附件也要求解析器、分块器或 Embedding 升级时建立新索引版本，回归评测后再切换。

你此前材料中的 pgvector 参数属于示例，不应直接当作 Milvus 配置使用。**Milvus 的索引类型、HNSW 参数和 SDK 配置，需要在确定版本后单独落实。**

---

## 八、生成、引用和回答状态

### 1. 证据包

交给模型的内容不能只是一串文本，还应附带：

**证据编号、文档、版本、章节、原始位置、适用条件和正文。**

同一问题需要不同方面的证据时，应覆盖不同子问题；不能用大量重复片段挤占上下文。发现冲突时保留冲突来源，不自动拼成一个没有依据的结论。

### 2. 回答状态

你明确提出的四种状态应保留：

| 状态                      | 含义              |
| ----------------------- | --------------- |
| `answered`              | 有充分证据回答         |
| `insufficient_evidence` | 当前可访问资料不足       |
| `needs_clarification`   | 缺少产品、版本、时间等关键条件 |
| `conflicting_evidence`  | 存在无法直接消解的证据冲突   |

附件还提出了 `not_authorized`、`out_of_scope`、`system_error`，可纳入完整接口契约，但要区分权限拒绝、业务范围和系统故障。对外提示不能泄露未授权文档的标题或存在性。 

### 3. 引用校验分为两层

**程序可以明确校验的内容：**引用 ID 是否属于本次证据、文档与版本是否存在、位置是否合法、用户是否仍有权限、内容是否已删除或失效。

**需要进一步评估的内容：**这段证据是否真的支持答案中的结论，而不是仅仅主题相近。

附件同时要求存在性、授权、定位和语义支持检查，也提醒不能把另一个模型的判断当成最终事实证明。

回答接口可以统一包含：

```text
request_id
status
answer
citations
quality
trace_id
```

其中 `quality` 展示证据充分性、引用覆盖和冲突情况，不把向量相似度直接包装成“答案正确概率”。

---

## 九、工程化、安全与知识生命周期

### 1. 处理状态与知识状态分开

你提出的入库任务状态可以保留：

```text
RECEIVED → PARSING → CHUNKING → EMBEDDING
         → INDEXING → VALIDATING → READY

任一步失败 → FAILED
```

附件还有知识生命周期状态，例如：

```text
active        当前允许使用
quarantined   质量、权限或安全不合格，隔离
superseded    已被新版替代
deleted       已撤销并进入删除清理
```

两者表达的不是一件事：**任务是否处理完成，不等于知识是否已经发布、当前有效或对某个用户可见。**

### 2. 更新与发布

保留你提出的流程：

**创建新版本 → 独立处理与建索引 → 验证 → 切换可见版本 → 归档或清理旧版本。**

MySQL 和 Milvus 之间不能按“天然拥有一个事务”来设计。项目需要明确发布协调和对账机制；查询时依据权威元数据检查当前有效状态。

附件要求新索引验证、版本切换、回滚，以及来源与索引之间的文档、Chunk、权限版本和删除记录对账。

### 3. 删除与权限撤销

需要覆盖原文访问、规范化文本、Chunk、向量、关键词索引、父片段、缓存和后续引用，而不只是删除上传记录。

按你的要求，应先撤销可见性，再执行清理；旧任务重试时必须检查当前文档状态，防止已删除文档被重新写入。备份与日志则需要结合保留策略处理。附件也将“删除不彻底”列为需要全血缘处理的风险。

### 4. 安全与故障处理

系统需要同时管理访问权限、模型出站数据、文件处理风险、敏感日志和调用预算。

故障时应安全降级：重排序失败可以返回基础检索排序；生成失败可以返回证据；引用验证失败不返回未经验证的生成答案；权限服务不可用则不能降级成无权限过滤的查询。

---

## 十、Vue 3 前端、配置与评测

### 1. 前端功能划分建议

下面是根据已有功能整理的页面范围，具体交互设计尚未确定。

| 页面或功能区      | 主要内容                   |
| ----------- | ---------------------- |
| **知识空间管理**  | 分类、来源、文档列表、权限范围        |
| **文档管理**    | 上传、版本、发布状态、删除、原文预览     |
| **处理任务**    | 解析和入库进度、失败原因、重试入口      |
| **检索调试**    | 原始问题、各路候选、融合与重排结果、过滤条件 |
| **问答界面**    | 答案状态、引用、来源定位、澄清与冲突提示   |
| **评测与运行管理** | 数据集、实验对比、质量、时延、费用和任务异常 |

前端负责呈现和提交操作，**最终权限判断在后端完成**，不能只靠隐藏按钮或分类实现访问控制。

### 2. 配置目录内容

| 配置类别 | 内容                           |
| ---- | ---------------------------- |
| 应用   | 环境、接口、日志、请求限制                |
| 数据存储 | MySQL、Milvus、文件存储连接与资源配置     |
| 模型   | 提供方、模型 ID、版本、地址、超时、并发、出站策略   |
| 解析   | 文件类型路由、MinerU、OCR、语音转写、清洗配置  |
| 切分   | 策略、Token 长度、重叠、父子片段规则        |
| 索引   | 维度、距离函数、索引类型、索引版本            |
| 检索   | 每路 Top-K、RRF、重排候选数、最终证据数量、预算 |
| 安全   | 权限策略、缓存隔离、敏感信息与保留策略          |
| 评测   | 数据集版本、评分配置、上线门槛、性能目标         |

模型密钥不直接写入普通配置文件、前端代码、提示词或日志。附件要求将密钥管理与普通配置、索引元数据分开。

### 3. 评测范围

| 层面        | 需要评估什么                                     |
| --------- | ------------------------------------------ |
| **知识加工**  | 解析成功率、正文覆盖、表格保真、空 Chunk、引用定位、更新与删除延迟       |
| **检索**    | Recall@K、Precision@K、MRR、nDCG、权限过滤、版本和时态命中 |
| **答案与引用** | 正确性、完整性、依据性、引用支持与覆盖、拒答、澄清、冲突识别             |
| **工程与成本** | 可用性、错误率、首字时间、完整回答延迟、Token、模型费用、安全测试        |

这些维度与附件的分层评测框架一致。

附件建议概念验证阶段采用 150—300 条人工样本，生产 MVP 扩展至 300—500 条；同时给出了首字时间、完整回答时间和可用性等示例目标。**这些属于待项目化的设计目标，不是系统已经达到的成绩，也不是必须无条件沿用的标准。** 

---

## 十一、当前仍需要确定的事项

| 待确定项               | 当前材料能够确认到什么程度                                        |
| ------------------ | ---------------------------------------------------- |
| **后端框架**           | 语言为 Python 3.12，框架未明确                                |
| **MinerU 部署与格式覆盖** | 接入目标明确；版本、运行环境、Word/PPT/PDF 处理路径和转换方案未明确             |
| **关键词/BM25 实现**    | 双路检索要求明确；在选定 Milvus 版本内如何实现，或是否需要配套组件，尚未决定           |
| **模型清单**           | 模型类别明确；LLM、OCR、语音、Embedding 和 Reranker 的最终型号、部署方式未确定 |
| **组件版本与兼容性**       | MySQL、Milvus、SDK 和解析依赖的版本及与 Python 3.12 的组合尚未验证      |
| **任务、存储与缓存**       | 能力要求明确；具体队列、文件存储和缓存组件未确定                             |
| **默认检索参数**         | 两份材料有不同区间，需要评测选定                                     |
| **业务容量与验收目标**      | 实际文档量、并发、更新时效、权限撤销时效、成本预算尚未明确                        |

这些缺口不影响模块划分，但会影响依赖安装、数据库设计细节、检索实现和部署方案。附件也要求正式实施前补齐真实数据、权限、容量和模型部署约束。

---

## 最终总结

你的需求已经可以形成一套清楚的工程架构：

**`ai` 接入能力，`document_processing` 加工文件，`knowledge_modeling` 管理知识，`vector_indexing` 建立索引，`information_retrieval` 提供召回，`rag_orchestration` 组织证据，`generation_control` 生成并验证答案，`engineering_security` 保证安全运行，`evaluation` 检验质量，`configs` 管理可调整参数。**

整个项目最重要的四条约束是：

**资料必须可追溯，知识必须有版本，检索必须受权限约束，答案必须由证据支持。**

在这个基础上，你选择的 **Python 3.12 + Vue 3 + MySQL + Milvus + 统一 AI 接入层**，已经有了明确的职责分工；下一步实现时，重点是把未确定的解析支持、BM25 组件、模型版本和参数配置落实，而不是继续增加更多功能模块。




# RAG 知识库：从知识体系到系统实现架构

你要开发的，不只是“上传文件后让大模型回答”，而是一个具备 **知识管理、检索、证据组织、答案生成、更新维护和质量评估** 的系统。

下面以 **Python 自研、中文文档为主、支持多个知识库、答案带出处、文档可更新和删除** 为目标展开。个人本地原型可以简化用户管理，但准备给多人使用时，权限隔离必须补齐。

首先明确：**RAG 是在回答时检索外部知识并提供给模型，不是把上传的文件重新训练进大模型。** 因而，文档处理、搜索和软件工程，与你是否熟悉大模型同样重要。([Microsoft Learn][1])

---

## 一、你需要掌握的完整知识架构

建议把知识体系分成下面九层。这里的“掌握程度”，是以你能够独立实现一个项目为标准，而不是要求把所有相关技术全部学完。

| 知识层                    | 需要掌握的具体内容                            | 应达到的实现能力                    |
| ---------------------- | ------------------------------------ | --------------------------- |
| **1. Python 与后端基础**    | 类型标注、异常处理、异步编程、HTTP、数据库操作、日志、测试、配置管理 | 能写一个稳定的上传、查询和流式回答服务         |
| **2. 数据与文档处理**         | 文件类型识别、文本提取、版面结构、表格解析、OCR、编码和清洗      | 能把原始文档转换成结构清楚、可定位出处的数据      |
| **3. 知识组织与建模**         | 知识分类、文档版本、章节、元数据、适用条件、权限、有效期         | 能解释每条知识“是什么、来自哪里、何时有效、谁能访问” |
| **4. 信息检索基础**          | 倒排索引、分词、BM25、向量相似度、Top-K、召回与排序       | 能分别实现关键词检索和语义检索，并判断各自的问题    |
| **5. Embedding 与向量索引** | 向量模型、输入规范、维度、距离函数、HNSW、近似检索          | 能建立向量索引，并处理模型更换、索引重建和过滤问题   |
| **6. RAG 检索编排**        | 问题处理、混合检索、结果融合、重排序、父子片段、上下文预算        | 能把最有用的证据送入模型，而不是直接拼接搜索结果    |
| **7. 生成与引用控制**         | 提示词、证据约束、结构化结果、引用校验、拒答与澄清            | 能返回有依据的答案，并在证据不足时正确停止       |
| **8. 工程化与安全**          | 后台任务、幂等、版本发布、鉴权、缓存、监控、部署、备份          | 能处理文档更新、任务失败、服务重启和越权访问      |
| **9. 评测与优化**           | 检索指标、答案正确性、依据性、引用质量、时延和费用            | 能证明一次改动确实改善了系统，而不是只凭感觉      |

**学习重点建议：先掌握后端、文档处理、检索和评测，再深入模型训练。** 对这个项目的第一版，我不建议把 LoRA、强化学习、多 Agent 和 Kubernetes 同时列为前置条件。

---

## 二、先确定系统总架构

建议采用 **模块化单体后端 + 独立入库任务进程**，不要一开始拆成很多微服务。

系统分成三部分。

### 1. 离线入库链路

这里的“离线”指不在用户问答请求中执行，不一定意味着断网。

```text
上传文件 / 同步数据源
        ↓
文件校验、去重、创建文档版本
        ↓
文档解析、结构提取、必要时 OCR
        ↓
清洗、章节识别、表格处理
        ↓
知识切分、补充元数据
        ↓
生成 Embedding
        ↓
建立关键词索引和向量索引
        ↓
质量检查、发布为可检索版本
```

### 2. 在线问答链路

```text
用户问题
    ↓
身份校验、确定允许访问的知识范围
    ↓
识别问题中的产品、版本、时间等条件
    ↓
关键词检索 + 向量检索
    ↓
结果融合、去重、重排序
    ↓
补充必要上下文，再校验权限和版本
    ↓
组织证据、检查是否足以回答
    ↓
大模型生成答案
    ↓
校验引用、返回答案与出处
```

### 3. 管理与质量链路

```text
知识库管理 / 文档版本 / 权限 / 入库任务
                    +
评测集 / 查询追踪 / 用户反馈 / 成本与时延监控
```

这种区分与标准 RAG 的两条主要流程一致：一条处理和索引知识，另一条检索知识、组织上下文并生成回答。第一版使用固定流程即可，不必让 Agent 动态决定每一步。([Microsoft Learn][2])

**实现建议：先把 `/search` 做好，再开发 `/ask`。** 前者只返回证据，后者才调用大模型。这样你能独立判断“没找到答案”和“找到但答错”这两类问题。

---

## 三、知识库里面的“知识”应该怎样组织？

这一部分是数据架构，不是模型架构。

### 1. 建议采用的知识层级

```text
租户 / 组织
└── 知识库
    └── 业务分类
        └── 文档
            └── 文档版本
                └── 章节 / 表格 / 内容块
                    └── 检索片段 Chunk
                        └── 可引用的原文位置
```

例如，下面只是一个结构示例：

```text
某团队
└── 产品支持知识库
    └── 安装与维护
        └── 示例设备维护手册
            └── 第 3 版
                └── 第 4 章：维护周期
                    └── 常规环境下的维护要求
                        └── 原始 PDF 第 12 页，指定段落
```

这里要区分：

**文档是管理单位，Chunk 是检索单位，原文位置是证据单位。**

不要只保存一条 `text + vector`，否则后面很难实现版本更新、引用定位、删除和权限控制。

### 2. 不同知识类型，需要不同的内容结构

建议在导入和人工维护知识时，采用下面的组织规范。

| 知识类型        | 建议包含的内容             | 主要支持的问题            |
| ----------- | ------------------- | ------------------ |
| **概念与术语**   | 名称、定义、别名、适用范围、相关概念  | “是什么”“两个概念有什么区别”   |
| **规则与制度**   | 适用对象、条件、限制、例外、生效时间  | “是否允许”“什么情况下适用”    |
| **操作流程**    | 前置条件、操作步骤、成功标准、异常处理 | “怎么做”“做到哪一步失败了”    |
| **产品与接口说明** | 产品版本、参数、输入输出、限制、错误码 | “支持什么”“接口为什么报错”    |
| **故障与案例**   | 现象、环境、原因、解决办法、验证方法  | “出现这个问题怎么办”        |
| **结构化数据**   | 字段定义、单位、时间范围、数据来源   | “数值是多少”“不同条件下如何比较” |

这是我建议的知识维护规范，不意味着所有原始文件都必须先人工改写。

对于自动处理的文档，可以尽可能提取这些字段；无法确认的字段保留为空或交给审核，**不要让模型猜出的适用条件直接变成权威元数据**。

### 3. 区分三种文本

建议每个片段保留三个逻辑层次：

| 文本形式             | 用途       | 示例                  |
| ---------------- | -------- | ------------------- |
| `original_text`  | 保留可核对的原文 | 原始段落、表格内容           |
| `retrieval_text` | 提高搜索效果   | 文档标题 + 章节路径 + 原文    |
| `context_text`   | 提供给生成模型  | 经过裁剪、带证据编号和必要上下文的内容 |

可以给检索文本补充标题、关键词或摘要，但**引用必须能够追溯到原始材料**。生成的摘要不能悄悄替代原文成为事实来源。

---

## 四、核心技术模块：具体要学什么、实现什么？

### 4.1 文档解析：把文件变成可靠的数据

#### 需要掌握的知识

重点不是“哪个库能读 PDF”，而是：

**文字顺序是否正确、结构是否完整、表格有没有被破坏、内容还能不能定位回原文。**

建议按文档类型制定处理策略：

| 文件类型           | 建议的处理方式             | 重点检查                 |
| -------------- | ------------------- | -------------------- |
| Markdown / TXT | 按标题、段落、代码块解析        | 标题层级、代码块完整性          |
| Word           | 提取段落、标题、表格等结构       | 样式不规范、表格与正文关系        |
| 有文本层的 PDF      | 优先直接提取文本和版面信息       | 双栏顺序、页眉页脚、断行         |
| 扫描 PDF / 图片    | 对没有可靠文本的页面或区域进行 OCR | 数字、单位、专有名词识别错误       |
| HTML           | 提取正文、标题、表格与来源       | 导航噪声、脚本、不可信内容        |
| Excel / CSV    | 保留字段、行列和单位          | 不要把需要精确统计的数据简单压成散乱文字 |

Docling 是一个可评估的解析工具，它支持多种文档格式、PDF 版面和阅读顺序、表格结构以及 OCR，也提供统一的文档表示。是否适合你的中文资料，仍需要用实际文件测试。([Docling][3])

#### 建议的解析输出

至少保留：

```text
文档 ID、版本 ID
内容块类型：段落 / 标题 / 表格 / 代码
章节路径
原始文本
页面或章节定位信息
表格标题、表头、单位
解析器及解析配置版本
异常或质量标记
```

**验收方式：** 抽取几份最复杂的文档，将解析结果与原文并排核对。先确认内容没有读错，再进入 Embedding 阶段。

---

### 4.2 Chunk 切分：让检索片段既准确又完整

#### 需要掌握的知识

需要理解以下几种切分方式：

| 方法     | 做法                 | 我建议的使用方式         |
| ------ | ------------------ | ---------------- |
| 固定长度切分 | 达到指定长度就切开          | 用作基线，便于快速实验      |
| 结构切分   | 按标题、段落、列表、表格切开     | 作为文档型知识库的默认方案    |
| 语义切分   | 根据语义变化确定边界         | 在结构不清楚时进行对照实验    |
| 父子切分   | 小片段用于搜索，大段落用于补充上下文 | 用于“搜得准但信息不完整”的问题 |

微软的文档处理资料也强调利用段落、表格和文档结构构造语义连贯的片段，而不是只进行任意长度切割。([Microsoft Learn][4])

#### 建议的切分规则

**规则类内容：** 尽量让规则、适用条件和例外一起出现。只检索到“允许”，却漏掉后面的“但以下情况除外”，答案可能完全相反。

**表格类内容：** 拆分表格时，子片段应保留表头、单位、表名和必要注释。单独一行数字通常无法形成可靠证据。

**代码与接口类内容：** 尽量保留函数、参数说明和对应示例的完整关系，不要把代码与解释随意拆开。

**父子片段：** 小片段命中后，只补充同一授权范围内、同一适用版本的父段落或相邻内容，不能无条件拼接整份文档。

#### 第一轮可以使用的实验参数

以下是**调试起点，不是通用最优值**：

| 参数        | 起点建议               |
| --------- | ------------------ |
| 普通正文片段长度  | 约 400—800 Token    |
| 连续正文的重叠区间 | 约 50—100 Token     |
| 完整短段落、FAQ | 可以不重叠              |
| 表格和代码     | 优先保证结构完整，必要时单独配置   |
| 超长片段      | 继续按段落或句子拆分，而不是静默截断 |

长度需要按所用模型的 Tokenizer 计算，不能把“字符数”和“Token 数”当成同一个指标。

**验收方式：** 随机抽取片段，只看该片段及其元数据，检查能否理解“它在说什么、适用于什么、来自哪里”。

---

### 4.3 Embedding 与向量索引：理解语义检索

#### Embedding、Reranker、LLM 是三种不同职责

| 组件               | 输入            | 输出    | 职责          |
| ---------------- | ------------- | ----- | ----------- |
| **Embedding 模型** | 问题或文档文本       | 向量    | 快速寻找语义相关内容  |
| **Reranker**     | 问题与候选片段组成的文本对 | 相关性分数 | 对少量候选结果重新排序 |
| **生成模型 LLM**     | 问题、证据和指令      | 答案    | 基于证据组织回答    |

Embedding 可以预先编码文档；Cross-Encoder 类型的 Reranker 则联合处理问题和候选文档，通常用于召回后的重排序，而不是直接扫描整个大语料库。([Sbert][5])

#### 需要理解的数学与索引知识

至少掌握：向量维度、点积、余弦相似度、归一化，以及精确检索与近似检索的区别。

余弦相似度的核心形式是：

$$
\operatorname{cosine}(q,d)=\frac{q\cdot d}{\|q\|\|d\|}
$$

对于向量索引，先理解 HNSW 即可：它利用多层图搜索近邻，需要在查询速度、召回、内存和构建成本之间权衡。以 pgvector 为例，`ef_search` 影响搜索候选范围，`m` 和 `ef_construction` 影响图的构建。([GitHub][6])

#### 必须记录模型与索引配置

建议至少记录：

```text
embedding_model_id
model_revision
embedding_dimension
输入预处理配置
是否使用查询指令或前缀
是否归一化
距离函数
chunking_version
index_generation
```

**相同维度不代表不同模型生成的向量可以混用。** 更换 Embedding 模型或改变影响表示的处理方式时，应建立新索引并验证，而不是简单替换查询端模型。

中文和多语言场景可以将 `BAAI/bge-m3` 作为候选之一。其官方模型卡列出多语言能力和 1024 维向量等规格；这里把它作为可测试的候选，不代表它对你的资料一定最好。([Hugging Face][7])

**验收方式：** 准备同义表达、专有名词、相似但不同的规则等问题，检查向量检索是否找到了正确证据。

---

### 4.4 混合检索与重排序：把相关证据排到前面

#### 为什么不建议只做向量检索？

关键词检索和语义检索关注的匹配方式不同。混合检索可以同时利用字面匹配与语义相似性，Elastic 的官方方案也将全文检索和向量检索结合使用。([Elastic][8])

在你的系统中，我建议特别测试两组问题：

**精确名称类：** 产品编号、错误码、合同或文档编号、接口名称。

**语义表达类：** 用户说“页面一直转圈”，文档写的是“请求超时或服务无响应”。

#### 推荐的检索流程

```text
原始问题 + 已确认的检索条件
            ↓
     ┌──────┴──────┐
关键词检索       向量检索
     └──────┬──────┘
         结果融合
            ↓
    去重、核验权限和版本
            ↓
       Reranker 重排序
            ↓
  选择证据、补充必要上下文
```

**中文分词需要单独测试。** Elasticsearch 的 Smart Chinese 插件支持简体中文及中英文混合文本分析，可以作为分词基线；产品编号、缩写和领域词仍要结合实际数据检查。([Elastic][9])

#### RRF：先按排名融合，而不是直接加分数

BM25 分数和向量检索分数通常不在同一尺度。RRF 根据各列表中的排名进行融合：

$$
\operatorname{RRF}(d)
=\sum_i \frac{1}{c+\operatorname{rank}_i(d)}
$$

其中，某个结果未出现在一个列表里，就不获得该列表的贡献；\(c\) 是排名常数。这样不必直接比较两个检索器的原始分数。([Elastic][10])

#### 第一轮检索参数

同样，这只是建议的基线配置：

| 阶段    | 起点建议                        |
| ----- | --------------------------- |
| 关键词召回 | 前 30 条                      |
| 向量召回  | 前 30 条                      |
| 融合并去重 | 保留约 30—40 条候选               |
| 重排序   | 对这些候选进行相关性评分                |
| 最终上下文 | 选择约 4—8 个有效片段，并受 Token 预算约束 |

可以将 `bge-reranker-v2-m3` 作为多语言重排序候选。它输出的是问题与片段的相关性分数；即使经过 Sigmoid 映射到 0—1，也不能直接解释为“答案有多少概率正确”。([Hugging Face][11])

**两个重要判断：**

**Reranker 无法挽救根本没被召回的证据。** 因而应先检查候选中有没有正确内容，再优化排序。

**权限过滤不能为了补足 Top-K 而关闭。** 例如 pgvector 的近似检索配合过滤条件，可能返回不足数量的结果；应考虑迭代扫描、检索参数或索引策略，而不是删掉权限条件。([GitHub][6])

---

### 4.5 上下文组织、生成与引用：让答案能够核对

不要把所有搜索结果直接拼成一大段文本。

我建议给模型的每份证据都带稳定标识：

```text
证据编号：E1
文档：示例设备维护手册
版本：第 3 版
章节：第 4 章 / 维护周期
位置：PDF 第 12 页
适用条件：由已确认的元数据提供
内容：原文片段
```

#### 上下文组织需要处理什么？

**相关性：** 与当前问题无关的段落不要进入上下文。

**完整性：** 规则相关的条件、例外、表头和单位应尽量保留。

**一致性：** 不应把不同产品、不同版本的规则混在一起生成一个结论。

**预算：** 为问题、指令、证据和输出分别留出空间，不要全部用于堆积历史聊天。

#### 建议的回答状态

接口不要只有一个 `answer` 字段。建议至少支持：

| 状态                      | 含义                 |
| ----------------------- | ------------------ |
| `answered`              | 有足够证据回答            |
| `insufficient_evidence` | 当前可访问资料不足以支持答案     |
| `needs_clarification`   | 缺少产品、版本、时间等关键条件    |
| `conflicting_evidence`  | 找到了冲突内容，不能直接给出唯一结论 |

回答可以包含部分已确认结论，同时明确哪些部分没有依据，不必只能“全部回答”或“全部拒答”。

#### 引用需要后端校验

建议要求模型引用本次提供的 `E1、E2……`，再由后端完成以下校验：

**引用编号是否存在；是否属于本次提供的证据；对应文档版本是否正确；用户当前是否仍有访问权限。**

出处链接由后端生成，不让模型自己编造文件地址或页码。

不过，**引用存在不等于引用支持结论**。答案是否忠于证据仍需要评测；微软的 RAG 评估也将检索质量和回答依据性分开处理。([Microsoft Learn][12])

---

### 4.6 知识更新与删除：不要只实现首次导入

建议把入库过程建模成状态机：

```text
RECEIVED
   → PARSING
   → CHUNKING
   → EMBEDDING
   → INDEXING
   → VALIDATING
   → READY

任一步失败 → FAILED，记录失败阶段与原因
```

#### 文档更新

我建议采用“新版本准备好后再发布”的方式：

```text
收到新版文档
    ↓
创建新的版本记录
    ↓
独立完成解析、切分和索引
    ↓
检查完整性和抽样检索结果
    ↓
切换可见版本
    ↓
旧版本保留、归档或按策略清理
```

不要在新版尚未准备好时就把旧版全部删掉。

对于数据库与搜索引擎分别存储的架构，**不要假设两个系统的更新天然具有同一个事务**。建议由元数据库维护有效版本，搜索结果进入模型之前再核验，避免读到已撤销或未发布内容。

#### 文档删除和权限撤销

建议先在权威元数据中撤销可见性，阻止继续使用，再执行索引、缓存和文件清理。

需要覆盖的不只是向量，还包括关键词索引、父片段、答案缓存、引用预览和原文件访问。

另外，失败任务重试时必须检查文档当前状态，避免“用户已经删除文档，但旧任务重试又把它写回索引”。

---

## 五、数据库和接口应该怎样设计？

### 1. 建议的数据模型

在已有用户或租户体系之外，可以设计下面这些核心对象。它们是逻辑模型，初期不必全部拆成独立物理表。

| 数据对象                          | 关键字段                          | 作用         |
| ----------------------------- | ----------------------------- | ---------- |
| `knowledge_bases`             | ID、租户、名称、访问策略、状态              | 管理知识库      |
| `documents`                   | ID、知识库、来源、所有者、当前版本、删除状态       | 管理逻辑文档     |
| `document_versions`           | 版本 ID、文件哈希、原文件位置、生效时间、解析版本、状态 | 管理文档历史与发布  |
| `chunks`                      | 片段 ID、版本 ID、父片段、原文、章节、位置、哈希   | 管理证据片段     |
| `index_generations`           | 模型版本、维度、切分配置、索引位置、发布状态        | 管理可重建的检索索引 |
| `access_grants`               | 主体、资源、读写权限、权限版本               | 控制访问       |
| `ingestion_jobs`              | 文档版本、处理阶段、重试次数、错误、更新时间        | 跟踪入库任务     |
| `rag_runs` / `retrieval_hits` | 查询、配置版本、候选证据、排序、耗时、结果状态       | 排查问答问题     |
| `eval_cases` / `eval_runs`    | 测试问题、参考证据、预期行为、配置、评测结果        | 做回归测试      |

**文档版本和索引版本要分开。** 同一份文档可能因为更换切分方法或 Embedding 模型而重新索引，但原文并没有变化。

### 2. 建议的核心接口

| 接口                                              | 职责                  |
| ----------------------------------------------- | ------------------- |
| `POST /knowledge-bases`                         | 创建知识库               |
| `POST /knowledge-bases/{id}/documents`          | 上传文档，返回文档 ID 和任务 ID |
| `GET /ingestion-jobs/{id}`                      | 查看处理进度和失败原因         |
| `POST /search`                                  | 返回证据，不调用生成模型        |
| `POST /ask`                                     | 返回答案、状态、引用和追踪 ID    |
| `GET /documents/{id}/versions/{version}/source` | 经过鉴权后查看对应原文         |
| `DELETE /documents/{id}`                        | 撤销文档可见性并启动清理        |

上传接口返回“已接收”，不应该等同于“已可检索”。前端需要展示入库状态，避免用户刚上传就提问，却不知道文档还没处理完成。

---

## 六、第一版的技术选型

下面给出一套可以落地的组合。选择它是为了职责清楚，不是要求你收集所有框架。

| 层面        | 建议选择                    | 说明                     |
| --------- | ----------------------- | ---------------------- |
| 后端 API    | Python + FastAPI        | 文档管理、搜索、问答和鉴权入口        |
| 元数据       | PostgreSQL              | 文档、版本、任务、权限、评测记录       |
| 文件存储      | 开发时本地目录，上线后对象存储         | 保存原文件和解析产物             |
| 文档解析      | 按类型选择解析器，可评估 Docling    | 先支持最常见的两三种文件           |
| 中文混合检索    | Elasticsearch           | 同一检索服务承载全文和向量搜索        |
| Embedding | 可替换的模型接口                | 以 BGE-M3 等模型作为候选评测     |
| Reranker  | 可替换的重排序接口               | 根据效果收益和延迟决定是否启用        |
| 生成模型      | 可替换的 LLM 接口             | 用真实中文任务比较，不绑定单一供应商     |
| 入库任务      | 独立 Worker + 持久化任务记录     | 任务量增加后可使用 Celery 等队列框架 |
| 测试与部署     | pytest + Docker Compose | 先完成可重复测试和单机部署          |

Elasticsearch 支持全文和向量的混合检索，因此采用这条路线时，不需要仅为了“有向量数据库”再叠加一个 Milvus。([Elastic][8])

**更轻的原型路线：PostgreSQL + pgvector。** 它适合减少初期组件；但关键词检索，尤其中文分析，需要作为独立能力设计，不能认为装了 pgvector 就已经具备完整混合检索。pgvector 本身提供的是 PostgreSQL 内的向量相似度搜索。([GitHub][13])

LangChain 等框架可以用于复用组件，但建议将业务接口写成自己的抽象，例如：

```text
DocumentParser
Chunker
EmbeddingClient
Retriever
Reranker
ContextBuilder
AnswerGenerator
AuthorizationService
```

这样后续换模型、换数据库，不需要改写整条业务链路。

---

## 七、上线前必须补齐的安全和工程能力

### 1. 权限不能只写在提示词里

建议由后端根据真实登录身份确定访问范围，并贯穿关键词检索、向量检索、父片段补充、缓存、引用预览和原文件读取。

**未授权内容不应先进入模型，再希望模型不要说出来。**

PostgreSQL 的行级安全可以作为防护的一部分，但超级用户、带 `BYPASSRLS` 的角色，以及通常情况下的表所有者，存在绕过行级策略的机制。因此不能只“开启 RLS”而不检查实际服务账号。([PostgreSQL][14])

### 2. 检索文档是不可信输入

文档可能包含“忽略此前要求”“输出其他资料”等指令，这属于间接提示词注入的风险。RAG 或提示词约束本身并不能彻底消除这类问题。([OWASP Gen AI Security Project][15])

对你的第一版，我建议默认只做检索与问答，不给文档内容触发任意代码、数据库写入或网络请求的能力。同时限制文件大小、类型和解析资源，避免执行文档中的脚本或宏。

使用外部 Embedding、Reranker 或 LLM 服务之前，还应明确这些资料是否允许传出当前环境。

### 3. 重任务不要挤在问答服务中

文档解析、OCR 和批量向量化建议放到独立 Worker。FastAPI 官方文档也建议，较重的计算或跨进程任务可以采用 Celery 等工具。([FastAPI][16])

另外，异步主要帮助处理 I/O 等待，不会自动让 CPU 密集的解析任务变快。([FastAPI][17])

建议同时实现任务超时、失败重试、幂等写入、取消和人工重跑。

### 4. 缓存必须考虑知识与权限变化

我建议第一版先不做复杂的答案缓存。确实需要时，缓存键至少考虑：

```text
租户或用户作用域
权限版本
知识库与索引版本
标准化问题
检索配置
模型与提示词版本
```

文档删除、权限撤销或索引更新之后，应防止旧答案继续返回。

### 5. 记录完整链路，但不要无差别记录敏感内容

建议每次问答记录一个 `trace_id`，关联检索条件、候选片段、排序、配置版本、模型耗时、Token 使用、引用和错误状态。

日志中的文档正文、用户问题和模型输入也可能包含敏感信息，应进行访问控制和必要脱敏。

---

## 八、怎样判断这个 RAG 知识库真的做好了？

### 1. 建立独立评测集

建议从约 50 条经过人工核对的问题起步，再持续补充。这是项目起步建议，不是行业门槛。

问题至少覆盖：正常问答、同义表达、精确编号、表格、跨段落条件、版本冲突、知识缺失、越权访问和文档删除。

一条评测数据建议保存：

```text
问题
允许访问的知识范围
查询时间或适用版本
预期行为：回答 / 澄清 / 证据不足
参考文档版本与原文位置
答案应包含的关键事实
不应出现的内容
```

**参考证据尽量绑定原文位置，不要只绑定 Chunk ID。** 否则更换切分方法后，评测集会跟着失效。

### 2. 分开评估检索和生成

官方 RAG 评估资料也区分了检索过程与回答依据性等维度。一个系统可能找到了正确文档但答错，也可能回答流畅却没有证据支持。([Microsoft Learn][18])

我建议采用以下口径：

| 指标              | 要衡量的问题                    |
| --------------- | ------------------------- |
| **Hit@K**       | 前 K 个结果是否至少命中一份正确证据       |
| **证据 Recall@K** | 回答所需的参考证据，实际找回了多少         |
| **排序质量**        | 最有用的证据是否排在靠前位置            |
| **答案正确性**       | 关键事实、条件、数字和版本是否正确         |
| **依据性**         | 答案中的结论是否被提供的证据支持          |
| **引用质量**        | 引用是否真实、定位正确，并且支持对应结论      |
| **拒答与澄清质量**     | 没有证据时是否停止，有证据时是否被错误拒绝     |
| **性能与费用**       | 端到端 p50 / p95、入库耗时、单次问答成本 |

评估证据召回时，要注意重叠片段可能覆盖同一条事实，不能把重复片段计为多份独立证据。

### 3. 按层定位问题

| 现象               | 优先排查                   |
| ---------------- | ---------------------- |
| 原文件里有答案，但解析结果没有  | 解析器、阅读顺序、表格、OCR        |
| 解析结果正确，但片段无法独立理解 | 切分、标题补充、条件和例外是否被拆开     |
| 正确片段不在召回候选中      | 分词、Embedding、过滤条件、召回数量 |
| 正确片段被召回，但排得很后    | 融合和重排序                 |
| 正确证据已进入上下文，答案仍错  | 问题理解、提示词、模型、证据冲突       |
| 答案用了旧规则          | 生效时间、版本发布、缓存失效         |
| 用户看到了不该看的内容      | 权限、索引过滤、父片段、缓存和原文接口    |

一个很有用的对照实验是：**直接把人工确认的正确证据交给模型。** 如果仍答错，优先查生成；如果能答对，优先查前面的知识处理和检索链路。

---

## 九、建议的开发顺序与阶段成果

评测应该从第一阶段就开始，而不是上线前最后补做。

| 阶段               | 具体开发内容                       | 阶段成果             |
| ---------------- | ---------------------------- | ---------------- |
| **阶段 1：知识处理**    | 文件上传、解析、原文保存、章节和来源定位         | 能查看解析结果，并定位回原文   |
| **阶段 2：独立检索**    | Chunk、元数据、关键词或向量基线、`/search` | 不调用 LLM，也能返回正确证据 |
| **阶段 3：问答闭环**    | 上下文组织、生成、引用、澄清和证据不足状态        | 每个重要结论能核对出处      |
| **阶段 4：知识生命周期**  | 增量更新、版本发布、删除、任务重试            | 更新和删除不会留下错误的可见知识 |
| **阶段 5：多人可用**    | 鉴权、权限隔离、日志脱敏、资源限制            | 不同用户只能访问授权资料     |
| **阶段 6：效果与性能优化** | 混合检索、重排序、参数实验、压测与费用分析        | 有固定测试集和优化前后对照    |

建议第一版暂缓复杂多 Agent、自动写业务系统、知识图谱和模型微调。等评测表明固定 RAG 链路确实无法满足需求，再针对具体问题增加能力；标准 RAG 和 Agentic RAG 的区别就在于，后者让模型动态决定检索等步骤，复杂度也随之增加。([Docs by LangChain][19])

## 最后归纳

这个项目最重要的知识架构可以压缩成：

**后端基础 → 文档理解 → 知识建模 → 信息检索 → 向量与重排序 → 证据生成 → 版本与权限 → 评测和运维。**

实现时始终检查四件事：

**知识是否处理正确，检索是否找到证据，回答是否忠于证据，系统是否只使用当前有效且有权限的知识。**

这四件事做好之后，再增加框架和高级算法，才有明确的收益判断依据。

[1]: https://learn.microsoft.com/en-us/azure/foundry/concepts/retrieval-augmented-generation?utm_source=chatgpt.com "Retrieval augmented generation (RAG) and indexes"
[2]: https://learn.microsoft.com/en-us/azure/architecture/ai-ml/guide/rag/rag-solution-design-and-evaluation-guide "Design and Develop a RAG Solution on Azure - Azure Architecture Center | Microsoft Learn"
[3]: https://docling-project.github.io/docling/ "Index - Docling"
[4]: https://learn.microsoft.com/en-us/azure/ai-services/document-intelligence/concept/retrieval-augmented-generation?view=doc-intel-4.0.0 "Retrieval-Augmented Generation (RAG) with Azure Document Intelligence in Foundry Tools - Foundry Tools | Microsoft Learn"
[5]: https://www.sbert.net/examples/cross_encoder/applications/README.html?utm_source=chatgpt.com "Cross-Encoders"
[6]: https://github.com/pgvector/pgvector "GitHub - pgvector/pgvector: Open-source vector similarity search for Postgres · GitHub"
[7]: https://huggingface.co/BAAI/bge-m3 "BAAI/bge-m3 · Hugging Face"
[8]: https://www.elastic.co/docs/solutions/search/hybrid-search "Hybrid search | Elastic Docs"
[9]: https://www.elastic.co/docs/reference/elasticsearch/plugins/analysis-smartcn "Smart Chinese analysis plugin | Elasticsearch Reference"
[10]: https://www.elastic.co/docs/reference/elasticsearch/rest-apis/reciprocal-rank-fusion "Reciprocal rank fusion | Elasticsearch Reference"
[11]: https://huggingface.co/BAAI/bge-reranker-v2-m3 "BAAI/bge-reranker-v2-m3 · Hugging Face"
[12]: https://learn.microsoft.com/en-us/azure/foundry/concepts/evaluation-evaluators/rag-evaluators "Retrieval-Augmented Generation (RAG) Evaluators for Generative AI - Microsoft Foundry | Microsoft Learn"
[13]: https://github.com/pgvector/pgvector?utm_source=chatgpt.com "pgvector/pgvector: Open-source vector similarity search for ..."
[14]: https://www.postgresql.org/docs/current/ddl-rowsecurity.html "PostgreSQL: Documentation: 18: 5.9. Row Security Policies"
[15]: https://genai.owasp.org/llmrisk/llm01-prompt-injection/ "LLM01:2025 Prompt Injection - OWASP Gen AI Security Project"
[16]: https://fastapi.tiangolo.com/tutorial/background-tasks/ "Background Tasks - FastAPI"
[17]: https://fastapi.tiangolo.com/async/?utm_source=chatgpt.com "Concurrency and async / await"
[18]: https://learn.microsoft.com/en-us/azure/foundry/concepts/evaluation-evaluators/rag-evaluators?utm_source=chatgpt.com "Retrieval-Augmented Generation (RAG) evaluators"
[19]: https://docs.langchain.com/oss/python/deepagents/retrieval "Retrieval - Docs by LangChain"

