{
  "name": "enterprise-rag-kb-g3-frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test:api": "node --test src/api.test.mjs src/fileHash.test.mjs",
    "test:unit": "vitest run src/App.test.js",
    "test:e2e": "playwright test",
    "test": "npm run test:api && npm run test:unit",
    "check": "npm run test && vite build"
  },
  "dependencies": {
    "@noble/hashes": "2.4.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-vue": "^5.2.0",
    "@vue/test-utils": "^2.5.0",
    "jsdom": "^26.1.0",
    "vite": "^6.1.0",
    "vitest": "^3.2.7"
  }
}
