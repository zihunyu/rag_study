# 相同问题此前能回答、现在无法回答：原因核对

后续修复及真实验收已完成，见 [最终验收记录](QA_FINAL_ACCEPTANCE_20260909.md)。本文保留下述时间点的诊断证据，“当前”指当时状态。

2026-09-09，香港时间。此次仅检查已保存的会话、问答回执、模型调用账本及代码；未修改业务代码、配置或知识库，未追加模型调用。

## 当前仍存在的失败：引用核验阻断

用户在 13:04:30 再次提出“抗抑郁药有哪些”，13:06:06 结束。运行编号：`01a0848e-1cf6-7519-b8b4-bc33215b70d0`。当前生成提示 v14、条件核验 v19，生成模型 `gpt-5.5`、核验模型 `gpt-5.6-sol`。

对照上午 11:03:36 已成功的同题运行 `01a0841c-ee0c-7fc7-be36-0449fd478a57`：

| 核对项 | 结果 |
| --- | --- |
| 文档版本 | 同为当前 PDF 第 7 版 `01a08350-9f51-78ee-9aec-07fa89289c62` |
| 证据集合 | 同样 50 份；证据编号、文档/版本、切片编号及正文 SHA-256 全部一致 |
| 答案中的药物表格 | 每一行文字与引用标记完全相同 |
| 最新答案的事实检查 | 8 条输入事实全部 `SUPPORTED` |
| 整体事实覆盖 | `answer_check.covered=true` |
| 冲突检查 | 已执行，未报告冲突 |
| 引用检查 | `citations_valid=false`，原因为 `uncited_factual_intro` |
| 上游 HTTP | 本次会话解析、检索相关调用、生成和核验均返回 200 |
| 条件核验 | 基础核验失败后提前返回，未启动条件批次；不能把本次称为条件核验已通过 |

两次导语分别是：

- 先前成功：“抗抑郁药可按作用机制/类别概括为以下几类及代表药物：”
- 本次失败：“抗抑郁药可以按作用特点分成几类，常见代表药物如下：”

两段导语都未单独挂引文，表格每行都有引用。当前核验将新导语视为需要独立引用的事实段落，因此拒绝了整份回答。材料和表格事实未发生退化；拒答的直接原因是引用完整性。语义相近的导语在不同运行中处理不一致，说明生成与核验之间的引用约定尚不稳定。提示和核验版本也已变化，仅凭这两条记录不能将差异唯一归因于某一行提示或模型随机性。

代码中有两个相关缺口：

1. `backend/src/ragkb/domain/table_citations.py:8` 的引用格式补齐仅处理整个事实清单来自一个证据编号的情况。本题使用 E4、E43 两个编号，直接保留原文。不能通过给所有段落随意添加两个引用来代替来源核验。
2. `backend/src/ragkb/application/qa.py:750` 只在 `condition_checks` 包含 `missing` 时启动一次补写。引用失败没有对应的有界修复流程；`model_http.py:1071` 在基础检查不通过时直接返回，随后 `qa.py:871` 将结果记录成 `insufficient_evidence` 并隐藏正文。这种状态名称也容易让用户误认为知识库缺资料。

本次故障与条件批次容量、核验超时、MinerU 正文丢失或图片模型调用无关。不能据此声称其他问题的准确性或完整性已得到证明。

## 截图中三条历史消息分别发生了什么

| 提问时间 | 问题 | 实际失败原因 |
| --- | --- | --- |
| 09-08 22:48:31 | 镇静催眠药有哪些 | 旧图片路径在 E2 中加入“本图从编号 4 开始、未显示 1–3”的覆盖限制；E1 实际已经提供 1–3。核验仍要求整篇回答声明内容不全，唯一遗漏项为 K1。这里把单张图的局部范围限制扩成了整道题的证据缺口。 |
| 09-09 08:55:39 | 镇静催眠药有哪些 | 检索正常、45 份证据、已生成草稿；08:56:21 调用旧核验模型 `gpt-5.4-mini` 收到 HTTP 400。旧记录只给出 `CLAIM_VERIFIER_PROTOCOL_INVALID`，页面因而显示“格式或引用校验不符合要求”。模型不受当前上游支持的诊断见此前修复记录，当前配置已换用 `gpt-5.6-sol`。 |
| 09-09 13:00:43 | 抗抑郁药有哪些 | 会话解析调用 `gpt-5.5` 连续四次收到 HTTP 429，13:00:59 结束；`resolved_question` 为空，`rag_run_id` 为空，尚未进入知识检索。 |

对应历史运行分别为 `01a0817f-8c6d-7743-898c-23a5aa1516a3`、`01a083aa-3146-7aa4-b9b9-9f4a9015716f`；13:00 失败只有会话轮次编号 `01a0848a-2f52-7bf9-8211-fd0e84ef998b`。

HTTP 429 已从实际发送记录确认，不能仅凭异常类名判断：本地预算超限也可能使用同一异常类。该时间窗四笔上游响应均为 429。13:04 的后续调用重新返回 200，因此不能继续断言接口一直处于限流。

历史消息不会因为程序修复自动重新执行；截图前两条使用的是较早实现，不能当作当前代码刚刚执行的结果。

## 会话入口仍有诊断缺口

`backend/src/ragkb/adapters/conversation_context.py:81` 的生产解析器只在没有历史消息时直接返回；只要有历史，即使“抗抑郁药有哪些”是自包含问题，也先调用模型解析。这一步发生在 `qa_service.ask()` 和完整答案缓存之前。因此核心问答或缓存曾经成功，仍无法保障已有会话中的这一前置步骤成功。

`backend/src/ragkb/infrastructure/conversation_service.py:307` 的兜底仅保存 `type(error).__name__`。会话解析错误没有生成问答回执，也没有把 HTTP 状态、稳定错误码和失败阶段写进普通问答的诊断结构。

`frontend/src/pages/ChatPage.vue:87` 的限流提示读取 `result.warnings` 或 `result.coverage_report.performance`；该轮没有 `result`，但有 `turn.error_code=ProviderRateLimited`。页面没有据此分类，最终落到第 100 行的“本轮处理未完成，可以重新提问”。上一轮对问答核心和验收的错误显示修复未覆盖这条会话前置路径。

## 结论与后续修复目标

当前可复现的问题首先是“内容已有来源支持，但导语引用失败会整篇拒答，且没有修复机会”。另一个独立问题是“会话解析对上游的额外依赖与异常诊断遗漏”。旧图片范围误判和旧模型 HTTP 400 是截图中的历史原因，不能与当前引用失败混为一谈。

后续应围绕这两条当前路径补齐实现与回归：统一导语/事实段落的引用约定，针对可修复的引用遗漏提供有界纠正并重新执行完整核验；会话解析失败保留具体阶段与错误码，审慎处理明确独立问题的前置依赖，保持真正追问的指代约束。不能通过取消事实、条件或冲突检查来获得通过。

上一轮的中间版本成功样本不代表最终版本的网页回归全部通过。本次新失败证明仍有未解决路径，应据此修正完成度判断。

机器对照记录：`artifacts/qa-speed-plan-20260909/latest-regression-comparison.json`。原始回执及只读诊断脚本保存在同目录，不应把未通过核验的原始草稿当作可发布答案。
