# 截图回答是否中途停止：只读核对结果

> 2026-09-09 更正：原先仅核对第 110 页的四项版本，漏查了本轮同样已检索到的第 9～10 页连续十项版本，因此“原文只有四项、内容完整”的结论错误，予以撤回。处理状态完成、前后端正文一致的事实仍成立，但不代表来源内容已完整覆盖。已对原 PDF 第 9、10、110、111 页做视觉核对，并着手修复清单覆盖与编号。后续结果见 `QA_SOURCE_LIST_COVERAGE_FIX_20260909.md`。

问题：镇痛药的使用原则。会话 `5e4d6a9fd652c0502966d13da99e4a1efa9f`，第 17 轮；问答运行 `01a08512-5a54-730c-8d63-aeb5bb49d962`。

## 已确认的结果

该轮于 2026-09-09 15:32:54（香港时间）完成，状态 `completed`，回答 `answered`、`verified=true`，没有错误码或警告。会话保存的正文与问答运行保存的正文完全相同，共 361 字符，末尾与截图一致，没有另存一段尚未显示的后续文字。

第 110 页“10 镇痛药使用原则”包含 (1)～(4)，随后是外层编号 11 的另一个主题。但这只是其中一处来源。同一文档第 9～10 页相应标题下有连续 1～10 条，本轮 E5 已含全部十条。原答覆盖了前四条，却遗漏后六条；不能凭前一处四条版本认定完整。

## 末尾为什么像只说了一半

末尾“相关限制”中的 `(4) 用药应个体化，剂量应根据患者需要由小到大 [E5]`，来自系统后处理追加的精确原文片段。它与 E5 中的短摘录逐字匹配，来源定位 page=9；正文则已经使用 E1（page=110）的较完整段落，包含“直至患者疼痛消失”“过严会导致用药不足”“注意患者的实际疗效”等后续文字。

因此，这个短尾巴是已覆盖内容被重复补入，并非该处还有剩余正文等待输出。最近加入的原文补齐流程在这类“短摘录与较完整摘录同时出现”的情况下，去重与引用合并仍不完善；原片段又没有结尾标点，使它更像被截断。

## 完成与中断证据

- 模型调用记录均为 HTTP 200，生成阶段完成后，还执行了正文重组、完整复核、条件补齐、再次复核、权限检查和引用检查；这些阶段均为 `ok`。
- 条件检查记录：23 项已检查，2 项覆盖、21 项不适用、0 项缺失。该统计只表明当时核验器按其判断范围完成了检查，没有证明整份编号清单的覆盖；本次更正已确认实际有遗漏。
- 前端 `citedText` 只把证据标记转为可点击引用，没有裁剪正文。截图末尾与后台保存的完整末尾一致。
- 这条成功记录只保存了性能摘要，没有保存原始模型响应和 `finish_reason`；当前文本生成适配器也没有显式检查该字段。因此不能声称已经直接读到原始停止原因 `stop`。目前证据足以确认这次尾部来自已完成的后处理，未发现超时、取消或前端丢失后文。

## 建议后续处理

1. 对已经由较完整来源覆盖的条款合并引用，避免再追加短摘录；继续保留完整核验。
2. 成功与失败运行都记录低成本完成元数据：模型停止原因、输出令牌数及上限、后处理动作和最终字符数。
3. 明确区分“本轮处理已完成”与“资料范围覆盖完整”，不要仅凭“已验证”使用户推断涵盖全部知识。

最初那次检查只读数据库、代码和原文，未调用模型、修改业务代码或改写历史回答；此描述不包含更正后的修复工作。最初诊断工件 `artifacts/qa-speed-plan-20260909/answer-completion-source-check.json` 保留用于追溯，不再作为全文完整性的证明。带知识库原文的完整快照留在本地私有诊断文件中。
