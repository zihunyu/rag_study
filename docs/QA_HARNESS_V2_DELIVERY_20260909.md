# Harness v2 交付记录

日期：2026-09-09。使用者为管理员和知识库负责人。

## 已交付

| 请求 | 实际行为 |
| --- | --- |
| 简化案例表单、关联原文、清单检查 | 以问题、预期行为、要点为主；每项绑定文件、版本、片段和精确摘录；高级参数折叠；检查清单数量、连续编号和文字重复。 |
| 逐项验收、人工记录 | 显示覆盖、遗漏、错误、待复核及对应证据；支持负责人逐项更正并留说明；每次复核追加存储，不覆盖历史。 |
| 文档出题、辅助评审 | 从有权读取的已发布原文生成候选题；导入后仍待确认。独立评审每批最多六项，受调用预算控制并持久化进度。 |
| 质量与速度比较 | 显示各要点改善或退化、覆盖数和问答耗时；漏答更快明确标为质量退化；费用累计共同案例的全部重试与恢复调用。 |

案例标准不进入知识库索引或被测问答请求。产品代码没有添加药物、产品名称或业务答案的固定规则。

## 实际模型检查

使用当前有效登录、现有文档的已发布版本和普通问答流程执行。回归案例
`REG-10-ITEMS-20260909` 的十个要点逐项绑定原文；沿用此前已核对过的十项资料。

| 检查 | 结果 | 调用 |
| --- | --- | --- |
| 选定原文生成一道候选题 | 任务完成，依据有效，已保存为候选题，未自动确认 | 1 次真实 HTTP 请求 |
| 新回答的完整问答与独立评审 | 九项程序检查全通过；模型逐项建议覆盖 10/10、遗漏 0、错误 0；最终状态仍为待人工复核 | 11 次真实 HTTP 请求 |
| 冻结的历史四项回答独立复核 | 覆盖 4/10，明确发现 P5～P10 六项遗漏 | 2 次真实 HTTP 请求 |

候选题任务：`d0e7217ef30bb138504bb62982f9f19da985`。
十项验收任务：`ecf39d283aebde4b9e6a8dad63826810a2ea`。
历史原问答运行：`01a08512-5a54-730c-8d63-aeb5bb49d962`。

[在本地工作台查看十项验收](http://127.0.0.1:5173/acceptance/01a07ab3-1a06-77a5-b220-912264c8f321?run=ecf39d283aebde4b9e6a8dad63826810a2ea)。

新验收任务实测约 151 秒，其中普通问答约 98.37 秒，剩余包含独立评审和任务处理。
这次验证的是验收功能，不能据此宣称问答已经提速。
历史回答是冻结数据的诊断重放，没有伪造为新的问答运行；它与新任务不是严格的在线性能 A/B 测试。
10/10 是独立模型建议，未代替负责人签署最终通过，也未在本次重新运行完整 24 题。

完整本地记录位于 `artifacts/harness-v2/live-results.json` 和
`artifacts/harness-v2/old-answer-review.json`，包含原文和回答，应按项目数据权限管理。

## 自动检查与恢复

- 最终专项后端：30 项通过，覆盖逐项原文权限、无效版本、虚构证据、候选状态、预算、人工复核及不可绕过的程序失败。
- 恢复回归：七要点分成 6+1；第二批中断后恢复仅执行余下一项，普通问答只执行一次，完成批次不重复调用。
- 比较回归：覆盖 10/10 降至 4/10，即使耗时 100 秒降至 30 秒，也明确判为质量退化；评审规则改变则不可直接比较。
- 成本回归：恢复前问答与恢复后评审调用一同累计；排除其他案例调用，不把恢复前的成本遗漏掉。
- 前端：11 项 API、1 项配置、76 项组件测试通过，生产构建通过。构建仍有既有的大资源包提示。
- Ruff、Mypy（260 个源文件）、两种认证模式 OpenAPI 快照检查通过。
- 此前执行完整后端集得到 1,464 通过、1 失败、7 跳过、8 未选择。唯一失败是新增测试缺少导入；
  已修正并在最终 30 项专项回归中通过。没有把未再次执行的完整测试集宣称为全绿。

侧边栏浏览器自动化连接返回 `nodeRepl.fetch request failed`，因此本次没有完成浏览器截图或真实点击验收。
页面逻辑经过组件测试，模型流程经过真实服务调用；两者不能代替视觉检查。

## 部署与兼容

MySQL 已应用 `create_acceptance_review_details`，失败项为零；已发布业务文档未重新导入。
旧案例保留兼容，未绑定逐项原文的要求留待人工复核。旧运行不补造模型逐项结论；不同验收版本不直接归因比较。
辅助模型全部建议通过后仍需负责人确认。来源更新、撤回或权限失效时隐藏历史原文及逐项证据，并提示重新核对。

操作与限额见 [工作台说明](QA_ACCEPTANCE_WORKBENCH.md)。
