# G0 阶段完成包（修订 3）

状态：`REVIEW_REQUESTED`  
日期：2026-08-31  
审核记录：`G0-review-1.md`、`G0-review-2.md`  
配置 revision：历史 G0 配置已在后续迁移中删除；当前权威入口为 `config/.env` 与
`config/.env.example`。契约 revision：`canonical-document:g0-v1`、`g0-adr-register-v3`、`plan:v1.2`

## 1. 修订结论

首轮审核的 6 项问题均已修复，配置报告现在满足：

- `schema_error_count=0`
- `decision_blocker_count=0`
- `gate_blocker_count=0`
- `gate_ready=true`
- `stub_development_ready=true`
- `user_blocker_count_for_requested_gate=0`

G0 技术基线已就绪。真实模型、MinerU、Milvus 和格式效果仍按 G1/G2/G3 条件阻断，
不属于本轮用户输入要求，也没有被 Stub 冒充验收。

第二次审核发现的唯一 Gate 映射问题也已关闭：ASR 的 provider/endpoint/model_id/Token
统一在 G2；LLM 的 endpoint/model_id/revision/timeout/max_concurrency/Token 统一在 G3。

## 2. 首轮审核项关闭证据

| # | 审核项 | 修复 | 证据 |
| --- | --- | --- | --- |
| 1 | Python 3.12、本地环境、mypy 不得 skip | 使用工作区 Python 3.12.13 建立 `.venv`；新增 `scripts/bootstrap.py`；安装 `.[dev]` 与类型包；质量门强制 3.12、Ruff、format、pytest、mypy | Python 3.12.13；30 tests；mypy 23 files 无问题；0 skipped |
| 2 | 进程环境占位密钥误判 | 进程环境与本地 env 共用 `_secret_value_is_configured`；空、`__FILL_ME__`、带引号占位、`deferred`、`not_applicable` 均未配置 | 新增进程环境参数化回归和“只报告存在、不泄漏值”测试 |
| 3 | ADR 与计划重基线 | ADR v3 冻结方向；后续迁移固定 Zilliz Cloud 中国区和 Python SQLite 队列；表格/音频并入 R1 | `docs/adr/README.md`、`完整开发计划.md` |
| 4 | 60 份真实样本 Gate 迁移 | G0 改为六类各 10 槽位的清单、责任、指标和 Harness；前五类 G1、音频 G2 真实硬门禁 | 样本清单现位于测试 fixtures；计划 60、真实 0；`real_acceptance=false` |
| 5 | 无 Docker 扫描覆盖不足 | 文件名扫描覆盖全仓；内容扫描覆盖根脚本、backend、frontend、scripts、deploy、`.github`、ci；显式排除历史计划和规则/测试自身 | 新增 4 个扫描测试；当前仓库 violation 0 |
| 6 | G0 报告与 `gate_ready` | 纠正环境结论，统一记录工作区 Python 3.12.13 证据；配置 `gate_ready=true` 后才重新申请 | 本报告与 `artifacts/g0/config-validation.json` |

### 2.1 第二次审核项

| 审核项 | 修复 | 回归证据 |
| --- | --- | --- |
| ASR 字段和 `ASR_API_KEY` 过早映射到 G1 | 全部改为 G2 | G1 报告不含 ASR；G2 包含 ASR endpoint/model/Token |
| LLM revision/timeout/concurrency 被通用 G2 规则命中 | 在通用规则前增加 LLM 专用 G3 规则 | G2 报告不含任何 LLM 条件；G3 首次包含全部六项 LLM 条件 |
| 缺少分 Gate 回归 | 新增 G1/G2/G3 三项配置报告测试，真实 Milvus 模式也纳入 G2 fixture | pytest 33 tests 全通过 |

## 3. G0 需求、WBS 与 FR 对应

| G0 工作 | WBS / FR | 实现与证据 |
| --- | --- | --- |
| 配置 Schema、加载、责任、Gate、条件密钥 | WBS-00、FR-OPS/AUD | 历史 G0 证据；当前由类型化 `config/.env` 加载和 `scripts/check_env.py` 取代 |
| Basic Stub 与 Real Integration 分层 | WBS-00/10 | 配置报告的 readiness profiles；G0 允许 Stub，真实服务按 Gate 条件阻断 |
| 供应商中立端口和本地适配器 | ADR-001/002/006/007 | `ragkb.contracts.ports`、本地文件、Fake 模型、权限与队列端口 |
| 原生启动 | FR-OPS | `run_backend.py`、`run_worker.py`、`run_mineru.py`、`scripts/*.py`、`npm run dev` |
| 六类 60 槽位采集计划与格式 Harness | WBS-00、FR-ING | 文档/扫描/Office/表格/音频清单、CanonicalDocument Schema、真实 Gate 路由 |
| Milvus BM25/ACL/watermark Harness | WBS-00、FR-IDX/RET/IAM | G1 本地词法适配器仅开发；G2 真实 Milvus 硬门禁 |
| 模型、容量、成本、安全合规 Harness | WBS-00、FR-RAG/OPS/AUD | 出站分类/区域/跨境负向、透明估算、路径穿越、原子写、全仓扫描 |

本阶段没有实现 G1 API、正式领域流程、真实持久队列、数据库迁移、真实索引或真实模型调用。

## 4. ADR、范围、WBS 与 Gate 重基线

### 4.1 技术决策

- 模块化单体 + 独立 Worker；R1 默认使用 SQLite/文件日志支持的 Python 本地持久队列。
- 后续配置迁移固定使用 Python SQLite 持久队列，并删除其他消息队列路径。
- MySQL 是目标控制面事实源，本地文件系统是内容事实源，Milvus 是可重建检索投影。
- 生产检索目标是 Milvus 2.6 原生 BM25 first + Dense；应用层 RRF。
- G1 本地词法/向量适配器只供开发，不能宣称 Milvus 验收；真实中文 Analyzer、ARRAY ACL、
  security watermark、质量、p95 和恢复是 G2 硬门禁。
- 解析器统一输出 CanonicalDocument；权限 deny-first、检索前过滤、输出前复核。

### 4.2 R1 范围与工期

XLS/XLSX/CSV、WAV/MP3/M4A 已并入 R1，不再保留独立扩展版本冲突。

- 工期：20 周旧基线改为 **24 周 / 12 Sprint**。
- 人力：平均 11.25 FTE，约 **270 人周**；6 FTE 时估 46—50 周。
- G1：W8，前五类真实难例（文本 PDF、扫描/图片、DOCX、PPTX、表格）各 10 份硬门禁。
- G2：W12，10 份音频 ASR/时间戳与真实 Milvus 检索硬门禁。
- G3/G4/G5/G6：W16/W22/W23/W24。
- 发布解析黄金集：原 160 + 表格/音频 70，合并为 **230 份起**。
- WBS 新增 `WBS-25 音频与结构化适配`，删除旧独立扩展格式 WBS。

### 4.3 60 槽位样本责任

G0 只要求清单与 Harness：`pdf_text`、`pdf_scanned_or_image`、`docx`、`pptx`、
`spreadsheet`、`audio` 各 10 槽位，责任角色 `qa_evaluation`。当前真实样本数为 0，
因此没有任何格式被声明为已支持；真实验收分别在 G1/G2。

## 5. Python 3.12 环境与质量证据

解释器：

`C:\Users\jcy\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`

Bootstrap：

```powershell
& 'C:\Users\jcy\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts/bootstrap.py
& '.\.venv\Scripts\python.exe' scripts/run_quality.py
```

`.venv` 使用 Python 3.12.13，已安装项目和 `.[dev]`：pytest 8.4.2、Ruff 0.16.5、
mypy 1.20.2、PyYAML/jsonschema 及对应类型包。

| 检查 | 结果 |
| --- | --- |
| Python 版本 / bootstrap | PASS；3.12.13；项目本地 `.venv` |
| 配置 Schema / Gate | PASS；`gate_ready=true`；0 blocker |
| pytest | PASS；33 tests |
| Ruff lint | PASS |
| Ruff format | PASS；53 files formatted |
| mypy strict | PASS；23 source files，0 issues |
| npm 安装/检查 | PASS；`npm ci` 0 vulnerabilities，`npm run check` PASS |
| 后端/Worker/MinerU/迁移入口 | PASS（入口/Wrapper 自检，不冒充真实服务） |
| 五类 Spike Harness | PASS；真实 acceptance 仍为 false |
| 跳过项 | 0 |

生成证据（版本控制忽略）：

- `artifacts/g0/config-validation.json`
- `artifacts/g0/environment.json`
- `artifacts/g0/quality-summary.json`
- `artifacts/g0/spikes/{mineru,milvus,models,capacity,security}.json`

## 6. 密钥、配置与用户输入边界

配置优先级：非敏感值 `user YAML > Stub`；密钥 `process environment > local env file`。
两种密钥来源使用完全相同的占位判断，报告只含变量名、来源和布尔状态。

- 本轮没有读取或输出任何密钥值。
- 当前 11 个受管变量都判为未配置；当前模式的 4 个条件必需名称是
  `MINERU_TOKEN`、`LLM_API_KEY`、`EMBEDDING_API_KEY`、`RERANKER_API_KEY`。
- 它们只阻断 G1/G2/G3 真实集成，不阻断 G0；本轮不要求用户填写。
- 后续配置迁移删除了对象存储密钥和本地向量库选择；真实 MySQL/Redis/Zilliz/OIDC 仅按对应 Gate 条件要求。
- 负责人、增长、法律保留、保存天数、预算和评测负责人推迟到 G4/发布治理，
  不阻止 G0/G1 Stub 开发。

Real Integration 的条件阻断保持不变：

| 集成 | 条件阻断 |
| --- | --- |
| MinerU 自建 | `ai_services.mineru.version`、本机包/健康探测、G1 真实样本 |
| MinerU 托管 | `MINERU_TOKEN`、供应商处理区域审批 |
| Embedding / Reranker | endpoint、model_id、条件 Token、区域审批；dimension/normalize 由探测回填 |
| LLM | endpoint、model_id、条件 Token、区域审批（G3） |
| ASR | provider、endpoint、model_id、区域审批；外部 provider 再要求 Token（G2） |
| MySQL / Milvus | 当前本地适配器；真实 Milvus 是 G2 硬门禁 |

## 7. 无 Docker 全仓扫描

- 文件名：递归扫描整个仓库；忽略 `.venv`、缓存、`data`、`artifacts`、`node_modules` 等生成目录。
- 内容：扫描根脚本及 `backend`、`frontend`、`scripts`、`deploy`、`.github`、`ci`。
- 排除：历史计划文档不参与命令内容扫描；规则实现与专用测试自身显式排除，避免自命中。
- 检测：禁止 Dockerfile/Compose 文件名、Docker 构建/运行/拉取/Compose 命令和 Testcontainers 依赖。
- 结果：当前 violation 0；文件名、根脚本、CI 和历史计划排除均有测试。

## 8. G0 Gate 逐项结论

| 退出条件 | 结论 |
| --- | --- |
| Python 3.12 项目环境和质量工具 | PASS |
| 配置 Schema、Stub、责任和安全密钥状态 | PASS |
| `gate_ready=true`、无 G0 用户阻断 | PASS |
| 12 项 ADR 与本地原生运行重基线 | PASS |
| Milvus BM25 first 生产目标与 G2 真实门 | PASS（方向/Harness）；真实效果留 G2 |
| Python 本地 SQLite 持久队列 | PASS（方向/端口）；实现留 G1 |
| R1 表格/音频、24 周 WBS/Gate/样本/风险 | PASS |
| 六类 60 槽位清单、责任、Harness | PASS；真实 0，不宣称支持 |
| 无 Docker 全仓扫描 | PASS |
| 全部可执行测试 | PASS；0 failed，0 skipped |

未解决 P0/P1 代码缺陷：无。后续真实集成阻断属于相应 Gate 的入口条件，不是 G0 缺陷。

## 9. 修订文件

- 环境/质量：`pyproject.toml`、`scripts/bootstrap.py`、`scripts/run_quality.py`、根 `README.md`；
- 密钥校验：`backend/src/ragkb/config/loader.py`、配置测试；
- Gate 映射：历史文件已由类型化 `.env` 条件校验取代；
- ADR/配置：`docs/adr/README.md`；当前唯一模板为 `config/.env.example`；
- 计划：`完整开发计划.md` 的范围、ADR、WBS、Gate、Sprint、样本、工期、依赖和风险；
- 样本：`backend/tests/fixtures/manifests/format-samples.yaml`、格式 Harness 与测试；
- 安全：全仓无 Docker 扫描和 `backend/tests/test_security_scan.py`；
- 报告/状态：本报告和 `docs/orchestration-state.yaml`。

数据库迁移：无。外部系统写入/费用：无。真实模型或 MinerU 托管调用：无。

## 10. 下一阶段边界

若审核返回 `APPROVED:G0`，才允许进入 G1。G1 可实现 Python 本地持久队列、正式
CanonicalDocument/Locator/Chunk、上传隔离和前五类真实格式门；真实 Milvus 与音频硬门
仍留在 G2。若返回 `CHANGES_REQUIRED:G0`，只修复列出的审核项。

STAGE_REVIEW_REQUESTED:G0
