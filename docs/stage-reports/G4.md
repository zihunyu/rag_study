# G4 本地验证准备包（修订 2）

状态：`REVIEW_REQUESTED_LOCAL_PREPARATION_REVISION_2`

配置范围重基线：`APP_SECRET_KEY` configured/valid；`ASR_ENABLED=false`，三键空白但
当前 nonblocking；G4 config `gate_ready=true`。原始范围 6x10，当前非 ASR 范围 5x10，
audio deferred。企业 IdP 继续 deferred。

边界：只使用合成 Fixture、离线 Stub、SQLite 和生成的临时数据；`real_acceptance=false`。
用户当前设置 `ASR_ENABLED=false`，ASR/audio deferred；历史契约与未来恢复入口保留，
但不继续开发、不声明支持，也不计入当前 G4 非 ASR ready。

## WBS-60A

- PDF/扫描 PDF/图片、DOC/PPT/DOCX/PPTX、HTML/Markdown/TXT、XLS/XLSX/CSV、
  WAV/MP3/M4A 路由与 locator contracts；
- 扫描/图片 OCR、旧 Office conversion 使用离线 Stub，质量报告明确
  `BLOCKED_REAL_VALIDATION`；音频 Stub 仅作为历史回归保留；
- CanonicalDocument 质量报告包含 parser revision、节点数、locator coverage、issue codes；
- 单文档质量报告与人工 review API/UI 已建立，review 具备 RBAC 和幂等；
- 发布 readiness 强制最新 quality report + 最新 APPROVED review + revision 一致；
  `BLOCKED_REAL_VALIDATION` 永远不能 serving；缺 review、非批准和旧 revision 均 409、零副作用；
- 合法 WAV/PNG/JPEG/GIF/TIFF/MP3/M4A 通过真实 upload API；截断/伪 magic 返回 422；
- 空质量输入固定 `node_count=0`、coverage=0、`EMPTY_DOCUMENT`，无除零；
- 原始完整范围为六类 60 份；当前非 ASR 范围只计算五类各 10 份、共 50 份。
  audio collection 标记 `deferred_by_user`，不产生 blocker；五类继续校验哈希、脱敏、授权、权利、分类和参考定位。

## WBS-70

- 8 个合成 Prompt injection Fixture 逐条执行 retrieval/QA/citation/egress/RBAC/
  tombstone/reference 安全链，8/8；hidden retrieval、cross-tenant text、forged citation、
  external egress 均为 0；
- 本地性能/容量/长稳 Harness 实际执行 upload/queue/Worker/review/publication/search/ask，
  两个数据规模（2/4）、两个并发（1/2）、两个 Top-K（1/5）和每规模 25 次 long-run；
  总 success=80、failure=0，记录 Python/平台/延迟/答案长度，`slo_claimed=false`；
- 成本计量与熔断器 Dry-run，`billable_request_performed=false`；
- SQLite/MySQL/Zilliz/本地血缘迁移与对账只读/plan-only；
- TemporaryDirectory 内建立真实 RuntimeComponents/Schema v11，生成 queue/lifecycle/reference/
  RAG/publication/lineage/本地文件，执行 SQLite backup 与安全文件副本恢复；验证 tombstone-first、
  reference revoke、current pointer、cleanup outbox=4、queue idempotency、RAG run、candidate ACTIVE、
  文件哈希；`real_project_data_touched=false`；
- Runbook、监控/告警契约、发布/回滚清单与外部输入清单已建立。

## 当前外部阻断

- 五类非 ASR 真实难例共 50 份；
- 真实模型计费授权；
- 外部迁移/清理/云端恢复授权；
- 企业 IdP 继续按用户决定 deferred。

本报告完成后仍只申请 `G4_LOCAL_PREPARATION` 审核，不宣称 Release Candidate，不进入 G5。

## 质量证据

命令：`python scripts/run_quality.py`

| 检查 | 结果 |
| --- | --- |
| G4 config | APP_SECRET_KEY configured=true；ASR_ENABLED=false，ASR 三键为空且当前 nonblocking |
| pytest | PASS；201 tests，0 failed，0 skipped |
| Ruff lint / format | PASS；191 files |
| mypy strict | PASS；87 source files |
| SQLite migration | PASS；Schema v11，27 tables |
| OpenAPI | PASS；v1.0.0，27 paths，BearerAuth |
| 当前五类非 ASR contracts | PASS；真实样本 0/50，Gate 保持 BLOCKED；原始完整范围 6x10，audio deferred |
| G4 local validation | PASS；8/8 injection chains；4 个泄露/绕过计数均 0 |
| 性能/容量/长稳 | PASS；代表系统路径 80 success/0 failure；`slo_claimed=false` |
| backup/restore | PASS；真实 Schema v11 临时运行时；全部持久状态与文件哈希一致 |
| 成本/熔断 | PASS；Dry-run；0 billable requests |
| Vue/Vite | PASS；3 behavior tests + production build |
| secret scan | PASS；0 findings；不扫描 `config/.env` |

STAGE_REVIEW_REQUESTED:G4_LOCAL_PREPARATION（修订 2）
